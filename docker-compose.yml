version: '2.12.2'

services:

  db:
    image: postgres:14.3-alpine3.16
    ports:
      - '5431:5432'
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: Pderfhm86
      POSTGRES_DB: app